# Task: KRO-120 - Auditoría i18n: Fix Namespaces/Keys

## Contexto
Auditoría i18n con problemas de namespaces y keys incorrectas en features.

## Análisis (del issue KRO-120)

### 🔴 Prioridad 1 - Keys/Namespaces Incorrectos (KRO-121)
1. `features/profile/profile-form.tsx`: Usa `useTranslation('common')` pero accede a keys de `profile`
2. `features/inquiries/inquiry-row-actions.tsx`: Accede a `words.*` que no existen en `inquiries.json`
3. `features/agents/agent-list.tsx`: Usa `words:delete` (incorrecto) → `actions.delete`

### 🟡 Prioridad 2 - Textos Hardcoded en Español (KRO-122)
- inquiry-row-actions.tsx: "Asignar a usuario", "Eliminar"
- property-columns.tsx: "Ver en mapa", "Eliminar"
- listing-columns.tsx: "Eliminar"

### 🟠 Prioridad 3 - Fallbacks en Toast Messages (KRO-123)
- agent-selector-client.tsx, inquiry-row-actions.tsx, user-columns.tsx, user-form.tsx

## Subtareas

- **[KRO-121] Fix Namespace/Keys Incorrectos** ← Prioridad 1
- **[KRO-122] Eliminar Textos Hardcoded** ← Prioridad 2
- **[KRO-123] Eliminar Fallbacks en Toast** ← Prioridad 3

## Referencias
- Skill: `.opencode/skills/frontend-developer/SKILL.md` - Sección 5: i18n
- Archivos JSON: `locales/en/*.json`, `locales/es/*.json`
